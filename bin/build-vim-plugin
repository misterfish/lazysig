#!/bin/bash

set -e

bindir=$(realpath --relative-to . "$(dirname "$0")")
parsedir="$bindir"/../parser
plugindir="$bindir"/../vim-plugin
built="$parsedir"/Main

. "$bindir"/functions.bash

USAGE="Usage: $0"

cmd "$bindir"/build-parser
cmd cp -a "$built" "$plugindir"/parser
